I do not want another paper tab. I want a local research memory that survives the semester.
peer has 17 commands because research is not one prompt: map, read, ask, cite, gap, outline, journal.
`peer map "my field"`: field overview, subfields, and a reading order saved under `~/.peer/`.
The research agent should not need shell access. peer is jailed to `~/.peer/` and has no bash tool.
BYOK is boring until you realize it means your spend, your files, your library.
A PhD workflow is already a CLI: Git, LaTeX, grep, PDFs, scripts. peer just lives there too.
The useful unit is not a chat answer. It is a paper note, a citation, a graph edge, and a trail.
Every `peer read <arxiv-id>` becomes an Obsidian-compatible note plus graph edges.
Local-first research software should leave plain files behind.
`peer ask` is for questions your own library can answer better than the open web.
I want my research tools to remember why I cared about a paper six months later.
`peer cite "claim"` returns BibTeX and a `\cite{...}` you can paste into the draft.
The closest sibling to peer is Claude Code, but the tools are research tools instead of coding tools.
A role-aware research agent should talk differently to a first-year PhD and a PI.
Apache-2.0, local `~/.peer/`, source install while alpha. Rough edges are visible by design.
The graph should compound: citations, embeddings, notes, and action history in one local loop.
`peer graph` turns your research trail into a local D3 knowledge graph.
`peer daily` is a morning arXiv pass ranked by what is already in your library.
The CLI is not nostalgia. It is the shortest path between an idea, a citation, and a note.
If research is a journey, your tools should keep the map.
